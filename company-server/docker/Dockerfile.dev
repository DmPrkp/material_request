FROM node:22-alpine

WORKDIR /usr/company-server

COPY package*.json ./
RUN npm install

COPY ./ ./

EXPOSE 4500

# db:migrate сам заводит базу company, если её нет (src/db/migrate.ts).
CMD ["sh", "-c", "npm run db:migrate && npm run start:dev"]
