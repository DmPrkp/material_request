FROM node:22-alpine

WORKDIR /usr/user-server

COPY package*.json ./
RUN npm install

COPY ./ ./

EXPOSE 4200

# db:migrate сам заводит базу user, если её нет (src/db/migrate.ts).
CMD ["sh", "-c", "npm run db:migrate && npm run start:dev"]
