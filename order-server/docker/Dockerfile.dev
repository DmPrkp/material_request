FROM node:22-alpine

WORKDIR /usr/order-server

COPY package*.json ./
RUN npm install

COPY ./ ./

EXPOSE 4100

# db:migrate сам заводит базу order, если её нет (src/db/migrate.ts).
CMD ["sh", "-c", "npm run db:migrate && npm run start:dev"]
