{
  "extends": "./tsconfig.json",
  // test/ — вне сборки: иначе корень компиляции расширится и артефакт уедет в dist/src/main.
  "exclude": ["node_modules", "dist", "test", "**/*.spec.ts", "drizzle.config.ts"]
}
